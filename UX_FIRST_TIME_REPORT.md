# Gipfel 管理系统 — 首次用户体验全流程报告

> 模拟者：最终用户代表  
> 模拟日期：2026-08-08  
> 项目：contract-manager (Electron 桌面应用)

---

## 模拟路径：安装 → 启动 → 登录 → 探索

---

### 第一步：安装与启动

我按照 README 的指引，运行 `npm install && npm run dev`。安装花了大约两分钟（antd、recharts、electron 等依赖很多），一切顺利。窗口弹出来了，深色界面，挺酷的。

**第一感受**：暗色界面很专业，看起来像一个正经的企业管理软件。窗口标题栏没有菜单，有种"沉浸式"的感觉。

---

### 第二步：面对登录页

我看到一个深色背景的登录框，正中间是 Gipfel Logo 和标题"基础设施合同与区域模拟管理"。

**困惑 #1**：我需要输入用户名和密码。但我第一次用这个软件，哪里来的账号？我尝试输入 `admin / admin`，提示"用户名或密码错误"。又试了 `admin / 123456`，还是错误。我盯着登录框看了半分钟，才注意到底部有一个小到几乎看不见的蓝色文字"注册账号"。

**感受**："注册账号"这个按钮太小了，颜色也很暗淡（12px 蓝色文字），完全没有"首次使用请点击这里注册"的引导。我差点以为这是个内部系统，需要管理员给我发账号。对于一个本地桌面应用来说，这种体验太像 SaaS 了——应该直接告诉我"欢迎首次使用，请创建您的管理员账号"。

### 第二步补充：注册

我点击了"注册账号"，页面多了个"确认密码"输入框。我输入了 `admin / admin123 / admin123`，提示"密码需包含字母和数字"——嗯，admin 里有字母有数字啊？哦，密码要同时包含字母和数字。改成 `admin123`，注册成功，自动切回登录模式。

**困惑 #2**：注册成功后，提示"注册成功，请登录"，然后切换回登录模式——但表单没有自动填充我刚注册的用户名和密码，我得重新输入。这个体验打断感很强。而且我没有收到任何"欢迎"的感觉，就像在一个冷冰冰的系统中完成了一次事务。

---

### 第三步：进入仪表盘

登录后，我看到了一个侧边栏布局的界面。左侧是导航菜单（首页/项目管理/分析中心/系统管理），右侧是内容区。我默认进入了"仪表盘"页面。

**困惑 #3**：仪表盘上显示了一些数据——3个区域、3个合同、4家公司、占地多少平方米。我心想"咦？我什么都没做，怎么就有数据了？"这让我感到困惑：这些数据是真的还是假的？是我创建的吗？我需要删除它们吗？

而且仪表盘上有一个"总碳排"显示为 0，这明显是个 bug（审计报告已指出 `total_carbon` 字段不存在）。

**感受**：种子的示例数据到底是帮助还是干扰？对新手来说，看到一堆不是我创建的数据反而增加了认知负担。我应该看到一个引导页，告诉我"系统已为您创建了示例数据，您可以从这里开始探索"或者干脆让我从零开始。

---

### 第四步：探索区域管理

我点击左侧"区域"，看到三个区域（A区、B区、C区），有表格和一些统计卡。我试着点了"创建区域"，弹出一个表单，要填名称、人口、人才、碳排放、承载力…

**困惑 #4**：承载力是什么？人才是什么？这些字段对我来说没有任何解释。我只能瞎填。而且创建区域后，幸福度显示"—"（没有数据），这让我不确定自己是否操作正确。

**感受**：创建表单缺少字段说明和 tooltip。作为一个"基础设施合同管理系统"，我应该被告知这些参数代表什么、它们之间有什么关系。

---

### 第五步：创建合同

我点击"合同管理"，看到了之前种子数据生成的三个合同。我点击"新建合同"——

**困惑 #5**：合同类型有8种：基建、开采、采购、劳动力雇佣、投资、拨款、销售、减碳。选哪种？每种类型下面要填的项目都不一样。我选了"基建合同"，然后要填大量字段——名称、类型、对方公司、区域、签约日期、甲方…下面还有个"基建项目"卡片，要点"+ 添加项目"添加具体基建内容。然后每个项目要填项目名称、数量、单价、占地面积…

这个表单太长了！我第一次用根本不知道哪些是必填、哪些是可选的。而且"对方公司"下拉框里已经有了4家种子公司，我是要选其中一个吗？如果我的合作公司不在列表里怎么办？

**感受**：合同创建流程缺少向导。应该先引导我选择合同类型，解释每种类型的用途，然后分步骤引导填写。当前的表单是一次性堆砌所有字段，对新手极不友好。

---

### 第六步：模拟计算（关键流程）

我进入"模拟计算"页面，看到四个步骤：选择区域 → 输入参数 → 显示结果 → 历史记录。

**困惑 #6**：我选择了一个区域（A区），然后看到一大串参数——人口、人才、碳排、基建新增、供应量、需求量、上期均价、本期均价、成本、利润、承载力、增长率。有一半参数已经预填了（来自区域数据），但"供应量"、"需求量"这些是什么？为什么是空的？

我看到"导入合同数据"按钮，点了之后提示"已导入：人口0、碳排放0"——因为合同的聚合方式是根据合同类型映射的（劳动力→人口，开采→碳排），我刚创建的基建合同对人口和碳排没有贡献。这让我很困惑：为什么导入合同后这些值还是0？

**感受**：模拟计算是核心功能，但它假设用户理解"合同类型→参数映射"的逻辑。对新手来说，这需要一个逐步引导的 wizard，解释"模拟计算是什么 → 需要哪些数据 → 数据从哪里来 → 结果怎么看"。

我强行填了一些数字，点击"运行模拟"，看到了结果——幸福度、就业率、成交价等。但这些数字对我有什么意义？我看了也看不懂。

**困惑 #7**：模拟计算后，我被告知"模拟完成"，但没有任何下一步的引导。我应该继续做什么？股票系统有没有同步？我怎么验证？

---

### 第七步：设置页股票 Token 配置

我进入设置页，看到"股票联动"卡片。

**困惑 #8**：这里有个输入框让我填"股票API管理员Token"。什么 Token？哪里来的？虽然有API地址（gipfel-trading-api.onrender.com），但没有告诉我怎么获取 Token。是去那个网站注册吗？是登录后拿到吗？还是需要管理员给我？

下面还有一行小字"映射：A区→JGONG · B区→JXIAO · C区→WULIU,YLIAO"，这对新手来说毫无意义——JGONG 是什么？股票代码吗？

**感受**：这是一个"开发者自己看得懂，用户完全看不懂"的配置。至少应该有一个"如何获取 Token"的链接或说明。而且股票联动功能在模拟计算完成后是静默同步的——用户如果不来设置页，根本不知道这个功能存在。

---

### 第八步：其他页面的空状态体验

我点了"基建计算"页面——看到选择区域后有空状态引导"选择区域以开始计算"和一个大图标，这是不错的引导。

但点击"占地面积"页面，如果没有任何基建合同，会显示总数都是 0，图表为空。没有任何引导文案。

"基建类型"页面显示了一个32行的表格（种子数据），但没有任何操作按钮。我不能添加、编辑、删除基建类型。这是一个纯只读字典。

"趋势分析"页面有引导——"选择一个区域查看趋势 · 数据来自模拟计算的历史记录"。但如果没跑过模拟，会提示"该区域暂无模拟记录"，并提供跳转到模拟计算的按钮。这是好的空状态设计。

**困惑 #9**：不同页面的空状态引导风格不统一。有的有引导（基建计算、趋势分析），有的没有（占地面积、基建类型），有的有数据但因为种子数据而永远不会触发空状态（区域、合同、公司）。

---

### 第九步：Gipfel 平台菜单

我点了"Gipfel 平台"菜单项——

**困惑 #10**：页面闪了一下就跳回了上一页！我完全不知道发生了什么。原来它是触发主进程打开一个外部网页（gipfel.ltd），然后自动导航回上一页。这个体验太糟糕了——我甚至不知道有没有成功打开，我应该看到一个提示"正在打开 Gipfel 平台…"或者在系统浏览器中确认已打开。

---

## 10 个最影响首次体验的问题

### 问题 1：登录页无首次使用引导 — 严重度：P0
**现状**：seed.ts 没有创建默认用户。新人看到登录框，不知道账号密码，"注册账号"链接极小且无引导文案。  
**建议**：检测是否是首次启动（无用户时），直接显示注册表单，标题改为"欢迎使用 Gipfel — 创建管理员账号"。

### 问题 2：注册后不自动登录，表单不预填 — 严重度：P1
**现状**：注册成功提示"请登录"，用户需要重新输入刚填的用户名密码。  
**建议**：注册成功后自动登录，或至少预填用户名。

### 问题 3：种子数据带来的"幽灵数据"困惑 — 严重度：P1
**现状**：首次启动自动创建 3 个区域、4 家公司、3 个合同、32 种基建类型。用户不知道这些数据是系统创建的还是从哪里来的。  
**建议**：首次启动显示 onboarding 引导页，告知用户"已创建示例数据，您可以删除或修改它们"。或提供"从空白开始 / 加载示例数据"的选项。

### 问题 4：模拟计算参数门槛极高 — 严重度：P1
**现状**：12 个参数一次性展示，字段名对新手不友好（"承载力"、"基建新增"），缺少解释。  
**建议**：设计分步向导：①选择区域 ②导入合同数据（并展示映射结果）③预览参数 ④运行。每步有简短说明。字段旁加 tooltip 解释。

### 问题 5：股票 Token 配置无获取指引 — 严重度：P1
**现状**：只有一个输入框和 API 地址，没有 Token 获取方式说明。  
**建议**：添加"如何获取 Token"的链接/说明文字。或提供一个"前往股票系统"按钮。

### 问题 6：合同创建表单太长，字段不分主次 — 严重度：P2
**现状**：所有字段一次性堆在表单中，不同合同类型的明细项差异巨大但无向导。  
**建议**：分步骤创建：基础信息 → 明细项 → 确认。每步只展示相关字段。

### 问题 7：Dashboard 引用不存在字段 — 严重度：P0
**现状**：`DashboardPage.tsx:100` 引用 `data.total_carbon`，但 `DashboardSummary` 类型无此字段，导致显示为 0。  
**建议**：要么添加此字段到 SQL 查询和类型定义，要么移除引用。

### 问题 8：股票联动结果不可见 — 严重度：P2
**现状**：模拟计算后自动静默同步股票系统。成功或失败用户都不知道。  
**建议**：模拟计算完成后显示同步状态（toast 提示"已同步到股票系统"），或在结果区域展示同步日志。

### 问题 9：Gipfel 平台菜单行为诡异 — 严重度：P2
**现状**：点击后触发 `open-gipfel-window` 然后 `navigate(-1)`，页面闪一下就回退，用户不知道发生了什么。  
**建议**：至少弹一个确认或提示"Gipfel 平台已在浏览器中打开"，不要静默跳转。

### 问题 10：碳排放对幸福度公式符号错误 — 严重度：P0
**现状**：`formula.handler.ts:956` 中 `0.2 * carbonPerCapita` 是正号，碳排放越高幸福度越高，违反设计意图。  
**建议**：改为 `- 0.2 * carbonPerCapita`。

---

## 整体体验总结

**好的方面**：
- 暗色 UI 设计专业，符合"决策驾驶舱"定位
- 侧边栏分组导航结构清晰
- 部分页面有空状态引导（趋势分析、基建计算）
- 密码安全做得好（PBKDF2 + 登录限流）

**需要改进的核心问题**：
- **Onboarding 缺失**：没有欢迎流程、没有操作引导、没有首次使用提示
- **概念门槛高**：区域→合同→模拟→股票的链条对新手不透明
- **反馈不足**：很多操作静默完成（股票同步），错误提示不够具体
- **公式 Bug**：碳排权重符号错误，直接影响模拟结果可信度
